<div class="py-3" id="<%= dom_id post %>">
  <h1 class="card-title">
    <%= post.title %>
  </h1>

  <h4>
   <em><%= post.description %></em>
  </h4>
  <hr>
  <p>
  By Andy Maldonado
  <br>
  <em>Posted: <%= post.created_at.strftime("%c") %></em>
  </p>
  <hr>
  <div class="card-body text-start" data-controller="richtext">
    <%= post.content %>
  </div>

</div>
